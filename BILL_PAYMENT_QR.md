# Bill Payment QR Code - คู่มือการใช้งาน

## 🎯 Overview

เพิ่มตัวเลือกการชำระเงินแบบ **Bill Payment QR Code** ที่เมื่อสแกนแล้วจะเป็นการโอนเงินเข้าบัญชีธนาคารโดยตรง

## ✨ Features

### 1. **PromptPay QR Code** (เดิม)
- โอนผ่านเบอร์โทรศัพท์
- รวดเร็ว สะดวก
- รองรับทุกธนาคาร

### 2. **Bill Payment QR Code** (ใหม่! ⭐)
- โอนเข้าบัญชีธนาคารโดยตรง
- แสดงข้อมูลบัญชีปลายทาง
- มีเลข Reference (Order ID)
- ตามมาตรฐาน Thai QR Payment

## 🏦 ข้อมูลบัญชีธนาคาร

```
ธนาคาร: กสิกรไทย (KBANK)
เลขบัญชี: 038-3-87041-0
ชื่อบัญชี: ท.ช. ธีรตม์ ดอกคูณ
Bank Code: 004
```

## 📱 วิธีใช้งาน

### ฝั่งผู้ใช้ (Frontend)

1. **เลือกวิธีชำระเงิน**
   - คลิกปุ่ม "QR Code"

2. **เลือกประเภท QR Code**
   - 💳 **PromptPay**: โอนผ่านเบอร์โทร
   - 🏦 **Bill Payment**: โอนเข้าบัญชีธนาคาร

3. **กดปุ่มชำระเงิน**
   - ระบบจะสร้าง QR Code ตามประเภทที่เลือก

4. **สแกน QR Code**
   - ใช้แอพธนาคารสแกน QR Code
   - ตรวจสอบข้อมูลบัญชีปลายทาง
   - ตรวจสอบจำนวนเงิน
   - ตรวจสอบ Reference Number (Order ID)

5. **โอนเงิน**
   - ยืนยันการโอนเงิน
   - รอ Transaction สำเร็จ

6. **ยืนยันการชำระเงิน**
   - กดปุ่ม "✓ ยืนยันการชำระเงิน"
   - ระบบจะบันทึกสถานะเป็น "ชำระเงินแล้ว"

## 🔧 API Endpoints

### 1. สร้าง PromptPay QR Code
```http
POST /payments/generate-qr
Authorization: Bearer <token>
Content-Type: application/json

{
  "orderId": "ABC12345",
  "amount": 1500
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "qrCodeDataURL": "data:image/png;base64,...",
    "qrCodeText": "00020101021129370016A000000677010111...",
    "expiresAt": "2025-11-05T16:00:00.000Z"
  }
}
```

### 2. สร้าง Bill Payment QR Code ⭐ NEW
```http
POST /payments/generate-bill-qr
Authorization: Bearer <token>
Content-Type: application/json

{
  "orderId": "ABC12345",
  "amount": 1500
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "qrCodeDataURL": "data:image/png;base64,...",
    "qrCodeText": "00020101021230...",
    "expiresAt": "2025-11-05T16:00:00.000Z",
    "accountInfo": {
      "accountNumber": "038-3-87041-0",
      "bankName": "ธนาคารกสิกรไทย (KBANK)",
      "accountName": "ท.ช. ธีรตม์ ดอกคูณ"
    }
  }
}
```

## 🎨 UI Components

### การเลือกประเภท QR Code

```tsx
┌─────────────────────────────────────────┐
│  เลือกประเภท QR Code                  │
├─────────────────┬──────────────────────┤
│   💳            │    🏦                │
│   PromptPay     │    Bill Payment      │
│   โอนผ่านเบอร์โทร  │    โอนเข้าบัญชีธนาคาร │
└─────────────────┴──────────────────────┘
```

### QR Code แบบ Bill Payment

```tsx
┌─────────────────────────────────────────┐
│  สแกน QR Code เพื่อชำระเงิน            │
│             ฿1,500.00                   │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │ ข้อมูลบัญชีปลายทาง                │ │
│  │ ธนาคารกสิกรไทย (KBANK)            │ │
│  │ 038-3-87041-0                     │ │
│  │ ท.ช. ธีรตม์ ดอกคูณ                │ │
│  │ Ref: ABC12345                     │ │
│  └───────────────────────────────────┘ │
│                                         │
│  [QR CODE IMAGE 256x256]                │
│                                         │
│  🕐 เหลือเวลา: 14:35 นาที              │
│  หมดอายุ: 5/11/2568, 16:00:00          │
│                                         │
│  [✓ ยืนยันการชำระเงิน]                 │
└─────────────────────────────────────────┘
```

## 🔐 Technical Details

### QR Code Payload Structure

Bill Payment QR Code ใช้มาตรฐาน **EMVCo QR Code Specification**

```
Payload Format:
┌─────────────────────────────────────────┐
│ Payload Format Indicator (000201)      │ Tag 00
│ Point of Initiation (010212)           │ Tag 01
│ Merchant Account Info (30XX...)        │ Tag 30
│   ├─ Application ID                    │ Tag 00
│   ├─ Account Number                    │ Tag 01
│   └─ Bank Code                         │ Tag 02
│ Currency Code (5303764)                │ Tag 53 (THB)
│ Transaction Amount (54XX...)           │ Tag 54
│ Country Code (5802TH)                  │ Tag 58
│ Merchant Name (59XX...)                │ Tag 59
│ Additional Data (62XX...)              │ Tag 62
│   └─ Reference Number (05XX...)        │ Tag 05
│ CRC (6304XXXX)                         │ Tag 63
└─────────────────────────────────────────┘
```

### CRC-16 Checksum

ใช้อัลกอริทึม **CRC-16/CCITT-FALSE** สำหรับคำนวณ checksum

```typescript
Polynomial: 0x1021
Initial Value: 0xFFFF
Input: Payload string (ก่อน CRC)
Output: 4-digit Hex (ต่อท้าย payload)
```

## 📝 ตัวอย่างการใช้งาน

### Example 1: สร้าง Bill Payment QR สำหรับ Order

```typescript
// Frontend
const handleGenerateBillQR = async () => {
  try {
    const response = await paymentService.generateBillQR(
      orderId,
      1500  // จำนวนเงิน 1,500 บาท
    )
    
    setQrCodeDataURL(response.data.qrCodeDataURL)
    setAccountInfo(response.data.accountInfo)
    
    console.log('Bill Payment QR Created!')
    console.log('Account:', response.data.accountInfo.accountNumber)
    console.log('Bank:', response.data.accountInfo.bankName)
    console.log('Reference:', orderId)
  } catch (error) {
    console.error('Failed to generate QR:', error)
  }
}
```

### Example 2: ทดสอบด้วย Postman

```bash
# 1. Login
POST http://localhost:3000/auth/login
{
  "email": "user@example.com",
  "password": "password123"
}

# 2. Create Order
POST http://localhost:3000/orders
Authorization: Bearer <token>
{
  "orderId": "TEST001",
  "items": [...],
  "paymentMethod": "qr",
  "subtotal": 1500
}

# 3. Generate Bill Payment QR
POST http://localhost:3000/payments/generate-bill-qr
Authorization: Bearer <token>
{
  "orderId": "TEST001",
  "amount": 1500
}

# 4. สแกน QR และโอนเงิน...

# 5. Confirm Payment
POST http://localhost:3000/payments/confirm
Authorization: Bearer <token>
{
  "orderId": "TEST001",
  "transactionId": "TXN1730815800123"
}
```

## 🎯 Use Cases

### Use Case 1: ลูกค้าต้องการโอนเข้าบัญชีโดยตรง
- เลือก "Bill Payment"
- สแกน QR Code
- แอพธนาคารจะแสดงข้อมูลบัญชีปลายทาง
- ยืนยันการโอนเงิน

### Use Case 2: ลูกค้าต้องการโอนแบบรวดเร็ว
- เลือก "PromptPay"
- สแกน QR Code
- โอนผ่านเบอร์โทร (รวดเร็วกว่า)

### Use Case 3: ต้องการเก็บ Reference Number
- ทั้ง 2 แบบมี Reference Number (Order ID)
- สามารถตรวจสอบการโอนเงินได้ในภายหลัง

## 🔍 Troubleshooting

### ปัญหา: QR Code ไม่แสดง
```
✓ ตรวจสอบว่า backend รันอยู่
✓ ตรวจสอบ console log มี error หรือไม่
✓ ตรวจสอบว่า order ถูกสร้างสำเร็จ
✓ ลองสร้าง QR Code ใหม่
```

### ปัญหา: สแกน QR Code แล้วข้อมูลไม่ถูกต้อง
```
✓ ตรวจสอบข้อมูลบัญชีใน payment.service.ts
✓ ตรวจสอบ Bank Code (KBANK = 004)
✓ ตรวจสอบว่าเลขบัญชีถูกต้อง (ไม่มีขีด)
✓ ตรวจสอบ CRC checksum
```

### ปัญหา: QR Code หมดอายุ
```
✓ QR Code หมดอายุใน 15 นาที
✓ กดปุ่ม "ชำระเงิน" ใหม่เพื่อสร้าง QR Code ใหม่
✓ Alert จะแจ้งเตือนอัตโนมัติเมื่อหมดอายุ
```

## 🚀 Deployment

### Environment Variables

```env
# Backend (.env)
PROMPTPAY_ID=0832264668
BANK_ACCOUNT_NUMBER=0383870410
BANK_CODE=004
BANK_ACCOUNT_NAME=ท.ช. ธีรตม์ ดอกคูณ
```

### การตั้งค่าสำหรับ Production

1. **เปลี่ยนข้อมูลบัญชีจริง**
   ```typescript
   // Nestjs/src/payments/payment.service.ts
   private readonly bankAccount = {
     accountNumber: '0383870410', // เลขบัญชีจริง
     bankCode: '004',             // KBANK
     accountName: 'ท.ช. ธีรตม์ ดอกคูณ', // ชื่อบัญชีจริง
   };
   ```

2. **ทดสอบ QR Code**
   - สแกนด้วยแอพธนาคาร
   - ตรวจสอบข้อมูลบัญชีปลายทาง
   - ทดสอบโอนจำนวนเล็กน้อย (1-10 บาท)

3. **เปิดใช้งาน Webhook** (ถ้ามี)
   - รับ notification จากธนาคารเมื่อมีการโอนเงิน
   - อัพเดทสถานะ Order อัตโนมัติ

## 📊 Comparison

| Feature | PromptPay QR | Bill Payment QR |
|---------|-------------|-----------------|
| **ความเร็ว** | ⚡ รวดเร็วมาก | 🟢 รวดเร็ว |
| **ข้อมูลบัญชี** | ซ่อนอยู่ | ✅ แสดงเต็มรูปแบบ |
| **Reference** | ✅ มี | ✅ มี |
| **รองรับธนาคาร** | ✅ ทุกธนาคาร | ✅ ทุกธนาคาร |
| **Use Case** | ทั่วไป | B2B, ต้องการใบเสร็จ |

## ✅ สรุป

### ไฟล์ที่ถูกสร้าง/แก้ไข

**Backend:**
- ✅ `Nestjs/src/payments/payment.service.ts` - เพิ่มฟังก์ชัน `generateBillPaymentQR()`
- ✅ `Nestjs/src/payments/payment.controller.ts` - เพิ่ม endpoint `/payments/generate-bill-qr`

**Frontend:**
- ✅ `React/src/services/paymentService.ts` - เพิ่มฟังก์ชัน `generateBillQR()`
- ✅ `React/src/components/Payment.tsx` - เพิ่ม UI เลือกประเภท QR Code

### ความสามารถใหม่

1. ✅ เลือกได้ว่าจะใช้ PromptPay หรือ Bill Payment
2. ✅ แสดงข้อมูลบัญชีปลายทางครบถ้วน
3. ✅ มี Reference Number (Order ID) ทุก QR Code
4. ✅ สร้าง QR Code ตามมาตรฐาน Thai QR Payment
5. ✅ รองรับทุกธนาคารที่มี Mobile Banking

---

**สร้างโดย:** GitHub Copilot  
**วันที่:** 5 พฤศจิกายน 2025
