# การย้ายระบบชำระเงิน QR Code มาที่ Backend (NestJS)

## สรุปการทำงาน

ระบบการชำระเงินด้วย QR Code PromptPay ได้ถูกย้ายจาก Frontend (React) มายัง Backend (NestJS) แล้ว โดยใช้วิธีการสร้าง QR Billing จริง

## ✅ สิ่งที่ได้ทำเสร็จแล้ว

### 1. Backend (NestJS)

#### ติดตั้ง Package
- ✅ `promptpay-qr` - สำหรับสร้าง QR Code PromptPay
- ✅ `qrcode` - สำหรับแปลง QR Code เป็นรูปภาพ
- ✅ `@types/qrcode` - TypeScript types

#### สร้าง Payment Module
```
Nestjs/src/payments/
├── payment.dto.ts        - Data Transfer Objects
├── payment.service.ts    - Business Logic
├── payment.controller.ts - API Endpoints
└── payment.module.ts     - Module Definition
```

#### API Endpoints ที่สร้างใหม่
1. **POST /payments/generate-qr** - สร้าง QR Code สำหรับการชำระเงิน
   - Input: `{ orderId, amount }`
   - Output: `{ qrCodeDataURL, qrCodeText, expiresAt }`
   - ต้อง Login (JWT Auth)

2. **GET /payments/status/:orderId** - ตรวจสอบสถานะการชำระเงิน
   - Output: `{ status, order }`

3. **POST /payments/confirm** - ยืนยันการชำระเงิน
   - Input: `{ orderId, transactionId }`
   - ต้อง Login (JWT Auth)

4. **POST /payments/cancel/:orderId** - ยกเลิกการชำระเงิน
   - ต้อง Login (JWT Auth)

#### อัพเดท Order Schema
เพิ่มฟิลด์ใหม่:
- `paymentStatus` - สถานะการชำระเงิน (pending, paid, cancelled, expired)
- `qrCodeData` - QR Code Data URL
- `paymentExpiry` - เวลาหมดอายุของ QR Code (15 นาที)
- `transactionId` - Transaction ID จากธนาคาร
- `paidAt` - วันที่ชำระเงิน

### 2. Frontend (React)

#### สร้าง Payment Service
- ✅ `React/src/services/paymentService.ts` - Service สำหรับเรียก Payment API

#### อัพเดท Payment Component
- ✅ แก้ไข `Payment.tsx` ให้เรียก Backend API เพื่อสร้าง QR Code
- ✅ แสดง QR Code จริงจาก Backend แทนที่จะเป็น Mock
- ✅ เพิ่มปุ่มยืนยันการชำระเงินหลังสแกน QR
- ✅ แสดงเวลาหมดอายุของ QR Code
- ✅ Loading state สำหรับการสร้าง QR Code

## 📝 วิธีการทำงาน

### Flow การชำระเงินด้วย QR Code

1. **ผู้ใช้เลือกชำระด้วย QR Code**
   - เลือกธนาคาร (KBANK, SCB, BBL, KTB)
   - กดปุ่ม "ชำระเงิน"

2. **Backend สร้าง Order และ QR Code**
   - บันทึก Order ลง Database
   - สร้าง QR Code PromptPay พร้อมจำนวนเงิน
   - กำหนดเวลาหมดอายุ 15 นาที
   - บันทึก QR Code ลง Order

3. **แสดง QR Code ให้ผู้ใช้สแกน**
   - แสดงรูป QR Code แบบเต็มจอ
   - แสดงจำนวนเงินและเวลาหมดอายุ
   - รอให้ผู้ใช้สแกนและโอนเงิน

4. **ยืนยันการชำระเงิน**
   - ผู้ใช้กดปุ่ม "ยืนยันการชำระเงิน" หลังโอนเรียบร้อย
   - อัพเดทสถานะ Order เป็น "paid"
   - บันทึก Transaction ID
   - Redirect ไปหน้า Success

## 🔧 การตั้งค่า PromptPay

### แก้ไขเบอร์ PromptPay ของร้านค้า

แก้ไขในไฟล์ `Nestjs/src/payments/payment.service.ts`:

```typescript
export class PaymentService {
  // เปลี่ยนเป็นเบอร์ PromptPay ของคุณ
  private readonly promptpayId = '0861234567'; // ← เปลี่ยนที่นี่
  ...
}
```

## 🚀 วิธีใช้งาน

### เริ่มต้น Backend
```bash
cd Nestjs
npm install
npm run start:dev
```

### เริ่มต้น Frontend
```bash
cd React
npm install
npm run dev
```

## 📱 ทดสอบระบบ

1. ไปที่หน้า Cart และเพิ่มสินค้า
2. ไปที่ Checkout → Shipping
3. เลือก Payment Method = "QR PromptPay"
4. กดปุ่ม "ชำระเงิน"
5. QR Code จะถูกสร้างจาก Backend และแสดงขึ้นมา
6. สแกน QR Code ด้วยแอพธนาคารของคุณ
7. โอนเงินตามจำนวนที่แสดง
8. กดปุ่ม "✓ ยืนยันการชำระเงิน"
9. ระบบจะ Redirect ไปหน้า Success

## ⚠️ หมายเหตุสำคัญ

### สำหรับการใช้งานจริง (Production)

1. **Webhook จากธนาคาร**
   - ในการใช้งานจริง ธนาคารจะส่ง Webhook มาเมื่อมีการโอนเงิน
   - ควรสร้าง endpoint สำหรับรับ Webhook จากธนาคาร
   - อัพเดทสถานะ Order อัตโนมัติเมื่อได้รับ Webhook

2. **ตรวจสอบสถานะแบบ Polling**
   - สามารถใช้ตรวจสอบสถานะทุก ๆ 5 วินาที ผ่าน `GET /payments/status/:orderId`
   - แทนที่จะให้ผู้ใช้กดยืนยันเอง

3. **QR Code หมดอายุ**
   - QR Code จะหมดอายุใน 15 นาที
   - ควรเพิ่มระบบแจ้งเตือนเมื่อใกล้หมดอายุ
   - สามารถสร้าง QR Code ใหม่ได้

4. **Security**
   - ใช้ HTTPS ในการส่งข้อมูล
   - ตรวจสอบ JWT Token ทุก request
   - Validate จำนวนเงินที่ตรงกับ Order

## 🎯 ความแตกต่างจากเดิม

### เดิม (Frontend)
- สร้าง QR Code แบบ Mock ใน React
- ไม่มีการเก็บข้อมูล QR Code ใน Database
- ไม่มีการตรวจสอบสถานะการชำระเงิน

### ใหม่ (Backend)
- ✅ สร้าง QR Code PromptPay จริงใน Backend
- ✅ เก็บข้อมูล QR Code และสถานะใน Database
- ✅ มีระบบตรวจสอบสถานะการชำระเงิน
- ✅ กำหนดเวลาหมดอายุของ QR Code
- ✅ มีระบบยืนยันการชำระเงิน

## 📚 ไฟล์ที่ถูกสร้างหรือแก้ไข

### ไฟล์ใหม่
1. `Nestjs/src/payments/payment.dto.ts`
2. `Nestjs/src/payments/payment.service.ts`
3. `Nestjs/src/payments/payment.controller.ts`
4. `Nestjs/src/payments/payment.module.ts`
5. `React/src/services/paymentService.ts`

### ไฟล์ที่แก้ไข
1. `Nestjs/src/orders/order.schema.ts` - เพิ่มฟิลด์ payment
2. `Nestjs/src/app.module.ts` - เพิ่ม PaymentModule
3. `React/src/components/Payment.tsx` - อัพเดท UI และ Logic

## 🎨 ตัวอย่าง QR Code ที่สร้าง

QR Code ที่สร้างจะเป็นรูปแบบ PromptPay QR Payment มาตรฐานของธนาคารแห่งประเทศไทย:
- รองรับทุกธนาคารที่มี PromptPay
- มีจำนวนเงินฝังอยู่ใน QR Code แล้ว
- ผู้ใช้ไม่ต้องพิมพ์จำนวนเงินเอง

---

**สร้างโดย:** GitHub Copilot
**วันที่:** 5 พฤศจิกายน 2025
