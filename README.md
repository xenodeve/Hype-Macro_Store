# 🎮 HYPE-MA### 🚀 **Advanced Features**
- 🔄 **Futuristic Loading Page** - หน้าโหลดแบบ cyberpunk พร้อม morphing text effects
- 🎦 **YouTube Video Carousel** - คาร์รูเซลวิดีโอแบบอัตโนมัติพร้อมการควบคุมด้วยตนเอง
- 🎬 **Random Video Background** - พื้นหลังวิดีโอแบบสุ่มเพื่อประสบการณ์ที่น่าประทับใจ
- ✨ **Smooth Animations** - แอนิเมชั่นที่ลื่นไหลและสวยงาม
- 🧩 **Modular Architecture** - โครงสร้างโค้ดแบบแยกส่วนเพื่อการบำรุงรักษาที่ง่าย
- 🎯 **Smart Hover Manager** - ระบบจัดการ hover conflicts ระหว่างกลุ่มต่างๆ อัตโนมัติte - Premium Gaming Mouse Website

เว็บไซต์แสดงเมาส์เกมมิ่งพรีเมียมที่พัฒนาด้วย **Vite + TypeScript** พร้อมด้วยระบบ **Loading Page** และ **Modular Architecture** ที่ทันสมัย

## ✨ คุณสมบัติเด่น

### 🎨 **Modern Web Technologies**
- ⚡ **Vite Build System** - เครื่องมือ build ที่รวดเร็วพร้อม Hot Module Reload
- 🔷 **TypeScript Integration** - Type safety และ IntelliSense support ครบถ้วน
- 📱 **Fully Responsive** - ใช้งานได้ลื่นไหลในทุกอุปกรณ์และขนาดหน้าจอ
- 🌓 **Dark/Light Mode** - รองรับโหมดมืดและสว่างพร้อมการปรับตามระบบอัตโนมัติ

### 🚀 **Advanced Features**
- 🔄 **Futuristic Loading Page** - หน้าโหลดแบบ cyberpunk พร้อม morphing text effects
- 🎦 **YouTube Video Carousel** - คาร์รูเซลวิดีโอแบบอัตโนมัติพร้อมการควบคุมด้วยตนเอง
- 🎬 **Random Video Background** - พื้นหلังวิดีโอแบบสุ่มเพื่อประสบการณ์ที่น่าประทับใจ
- ✨ **Smooth Animations** - แอนิเมชั่นที่ลื่นไหลและสวยงาม
- 🧩 **Modular Architecture** - โครงสร้างโค้ดแบบแยกส่วนเพื่อการบำรุงรักษาที่ง่าย

## 💻 เทคโนโลยีที่ใช้

- **Vite** - Modern build tool สำหรับการพัฒนาที่รวดเร็ว
- **TypeScript** - ภาษาหลักสำหรับ type safety และการพัฒนาที่มีประสิทธิภาพ
- **HTML5** - โครงสร้างหน้าเว็บที่ทันสมัย
- **CSS3** - การจัดแต่งและแอนิเมชั่นขั้นสูงพร้อม custom properties
- **Tailwind CSS** - เฟรมเวิร์ค CSS สำหรับการพัฒนาที่รวดเร็ว
- **Font Awesome & RemixIcon** - ไอคอนสวยงามและครบครัน
- **Google Fonts** - ฟอนต์ IBM Plex Sans Thai สำหรับการอ่านที่ชัดเจน

## 📁 โครงสร้างโปรเจ็กต์

```
Hype-Macro Vite/
├── public/
│   └── vite.svg                # Vite logo
├── src/
│   ├── components/
│   │   ├── Animations.ts        # Animation controllers และ intersection observers
│   │   ├── HoverManager.ts      # ระบบจัดการ hover conflicts ระหว่างกลุ่ม
│   │   ├── LoadingPage.ts       # ระบบ loading page แบบ cyberpunk
│   │   ├── Navigation.ts        # Mobile menu และ smooth navigation
│   │   ├── ThemeToggle.ts       # ระบบ dark/light theme switching
│   │   ├── VideoBackground.ts   # Random video background system
│   │   └── VideoCarousel.ts     # YouTube video carousel controller
│   ├── types/
│   │   └── index.ts             # TypeScript type definitions
│   ├── main.ts                 # Entry point หลัก
│   ├── style.css               # CSS หลักพร้อม loading animations
│   ├── typescript.svg          # TypeScript logo
│   └── vite-env.d.ts           # Vite type definitions
├── index.html                 # HTML หลัก
├── package.json               # Dependencies และ scripts
├── tsconfig.json              # TypeScript configuration
└── README.md                  # เอกสารนี้
```

## 🚀 การติดตั้งและใช้งาน

### ข้อกำหนดของระบบ

- Node.js (เวอร์ชั่น 16 หรือสูงกว่า)
- npm หรือ yarn

### การติดตั้ง

1. **Clone โปรเจ็กต์**
   ```bash
   git clone https://github.com/xenodeve/Hype-Macro_Vite.git
   cd Hype-Macro_Vite
   ```

2. **ติดตั้ง Dependencies**
   ```bash
   npm install
   ```

3. **รันในโหมด Development**
   ```bash
   npm run dev
   ```

4. **Build สำหรับ Production**
   ```bash
   npm run build
   ```

5. **Preview Production Build**
   ```bash
   npm run preview
   ```

## 🎯 หน้าและฟีเจอร์หลัก

### 🏠 **Loading Page**
- **Futuristic Design** - ออกแบบสไตล์ cyberpunk พร้อมเอฟเฟกต์พิเศษ
- **Morphing Text Animation** - ข้อความที่เปลี่ยนแปลงอย่างลื่นไหล
- **Progress Tracking** - แสดงความคืบหน้าการโหลดแบบ real-time
- **Smooth Transition** - การเปลี่ยนผ่านไปหน้าหลักที่ไม่มีสะดุด

### 🏠 **หน้าแรก (Hero Section)**
- **Random Video Background** - พื้นหลังวิดีโอแบบสุ่มจาก array
- **Animated Logo** - โลโก้พร้อมเอฟเฟกต์ glass morphism
- **CTA Buttons** - ปุ่มเรียกการกระทำพร้อมแอนิเมชั่น hover
- **Theme Toggle** - ปุ่มสลับโหมดสีพร้อมแอนิเมชั่น sun/moon

### ⭐ **ส่วนคุณสมบัติ (Features)**
- **Interactive Cards** - การ์ดคุณสมบัติพร้อม hover effects
- **Scroll Animations** - แอนิเมชั่นตามการเลื่อนหน้า
- **Product Specifications** - รายละเอียดเทคนิคของเมาส์

### 🎬 **ส่วนรีวิว (Reviews)**
- **YouTube Video Carousel** - คาร์รูเซลวิดีโอแบบอัตโนมัติ
- **Navigation Controls** - ปุ่มควบคุมการเล่นด้วย liquid glass effects
- **Auto-play System** - ระบบเล่นอัตโนมัติพร้อมการหยุดชั่วคราว

### 🛍️ **ส่วนสินค้า (Products)**
- **Product Grid** - กริดแสดงสินค้าแบบ responsive
- **Interactive Cards** - การ์ดสินค้าพร้อมเอฟเฟกต์ hover และ shadow
- **Dynamic Pricing** - แสดงราคาพร้อมสีที่แตกต่างกันตามรุ่น

## 🔧 คุณสมบัติเทคนิค

### 🏗️ **Modular Architecture**
```typescript
// Type-safe imports
import { VideoBackground } from './components/VideoBackground.js';
import { ThemeToggle } from './components/ThemeToggle.js';
import { Navigation } from './components/Navigation.js';
import { VideoCarousel } from './components/VideoCarousel.js';
import { Animations } from './components/Animations.js';
import { LoadingPage } from './components/LoadingPage.js';
```

### 🎨 **Loading Page System**
- **Class-based Architecture** - โครงสร้าง OOP สำหรับการจัดการ
- **Progress Simulation** - จำลองความคืบหน้าการโหลด
- **Morphing Text Effects** - เทคนิค CSS animation ขั้นสูง
- **Hardware Acceleration** - ใช้ transform3d สำหรับประสิทธิภาพ

### 🎪 **Video Carousel Features**
- **Auto-rotation** - หมุนวิดีโออัตโนมัติทุก 10 วินาที
- **Manual Control** - ปุ่มควบคุมด้วยตนเองพร้อม liquid glass design
- **Smooth Transitions** - การเปลี่ยนผ่านที่ลื่นไหลด้วย CSS transforms
- **YouTube API Integration** - ใช้ iframe API สำหรับการฝังวิดีโอ

### 🎯 **Smart Hover Manager System**
- **Conflict Detection** - ตรวจจับการ hover พร้อมกันระหว่างกลุ่ม
- **Priority-based Resolution** - แก้ไข conflicts ตาม priority level
- **Visual Feedback** - แสดง notification และ visual indicators เมื่อเกิด conflict
- **Auto State Management** - จัดการสถานะอัตโนมัติเมื่อเมาส์ออกจาก element

```typescript
// ตัวอย่างการใช้งาน HoverManager
import { initializeHoverManager, getHoverManager } from './components/HoverManager';

// เริ่มต้นระบบ
const hoverManager = initializeHoverManager();

// ตรวจสอบสถานะ
console.log(hoverManager.getGroupStates());

// รีเซ็ตสถานะทั้งหมด
hoverManager.resetAllStates();
```

## 🚀 Performance Optimization

- **Vite Hot Reload** - การโหลดซ้ำแบบรวดเร็วในระหว่างพัฒนา
- **Tree Shaking** - ตัดโค้ดที่ไม่ใช้ออกอัตโนมัติ
- **Code Splitting** - แยกโค้ดเป็นส่วนๆ สำหรับการโหลดที่เร็วขึ้น
- **TypeScript Compilation** - คอมไพล์เป็น JavaScript ที่เพิ่มประสิทธิภาพ
- **CSS Purging** - ลบ CSS ที่ไม่ใช้ใน production build

## 🎨 การปรับแต่ง

สามารถปรับแต่งเว็บไซต์ได้โดยการแก้ไขไฟล์ต่างๆ:

### 🎭 **การปรับแต่ง Loading Page**
```css
/* src/style.css */
.morphing-text::before {
  content: "LOADING";
  /* เปลี่ยนข้อความที่แสดง */
}

.loader {
  --loader-duration: 3s;
  /* ปรับระยะเวลาการโหลด */
}
```

### ⚙️ **การเพิ่มวิดีโอพื้นหลัง**
```typescript
// src/components/VideoBackground.ts
const videoUrls = [
  'your-new-video-url-1',
  'your-new-video-url-2',
  // เพิ่ม URL วิดีโอใหม่
];
```

### 🌈 **การตั้งค่า Theme Colors**
```css
/* src/style.css */
:root {
  --primary-color: #your-color;
  --secondary-color: #your-color;
  --accent-color: #your-color;
}
```

### 🎯 **การปรับแต่ง Hover Manager**
```typescript
// src/components/HoverManager.ts
// เพิ่มกลุ่ม conflict ใหม่
conflictingGroups: string[][] = [
  ['group1', 'group3'],
  ['group2', 'group4'], // เพิ่มคู่ใหม่
];

// ปรับ priority
const hoverGroup: HoverGroup = {
  element: htmlElement,
  isHovered: false,
  priority: 3 // priority สูงขึ้น
};
```

```css
/* ปรับแต่ง visual effects */
.hover-disabled {
  opacity: 0.3 !important; /* ปรับความทึบแสง */
  filter: grayscale(80%) !important; /* เพิ่ม grayscale */
}

.hover-conflict {
  border-color: rgba(255, 0, 0, 0.8) !important; /* เปลี่ยนสีขอบ */
}
```

## 🌐 การ Deploy

### **Vercel (แนะนำ)**
```bash
# เชื่อมต่อ GitHub repository
# เลือก build command: npm run build
# เลือก output directory: dist
# Deploy!
```

### **Netlify**
```bash
# Drag & Drop หรือเชื่อมต่อ Git
# Build settings: npm run build
# Publish directory: dist
```

### **GitHub Pages**
1. Build โปรเจ็กต์: `npm run build`
2. Deploy folder `dist` ไปยัง gh-pages branch
3. Enable GitHub Pages ใน Settings

## 🔍 Browser Support

| Browser | Version | Loading Effects | Video Carousel | Theme Toggle |
|---------|---------|----------------|----------------|--------------|
| Chrome  | 70+     | ✅            | ✅            | ✅           |
| Firefox | 65+     | ✅            | ✅            | ✅           |
| Safari  | 12+     | ✅            | ✅            | ✅           |
| Edge    | 79+     | ✅            | ✅            | ✅           |

## 🐛 การแก้ไขปัญหา

### **Debug Commands**
```bash
# ตรวจสอบ TypeScript errors
npm run build

# Dev server with verbose logging
npm run dev -- --debug

# Check bundle analysis
npm run build -- --analyze
```

### ปัญหาที่อาจพบ
1. **Loading page ไม่แสดง** - ตรวจสอบการ import LoadingPage.ts
2. **Videos ไม่เล่น** - ตรวจสอบ URL วิดีโอและ network connection
3. **Theme ไม่เปลี่ยน** - ตรวจสอบ localStorage permissions
4. **TypeScript errors** - รัน `npm run build` เพื่อตรวจสอบ type errors
5. **Animations กระตุก** - ตรวจสอบ CSS `will-change` properties

## 🏆 Features Showcase

### 🎮 **Gaming-Focused Design**
- เหมาะสำหรับ target audience ที่เป็นเกมเมอร์
- สีสันและธีมที่สะท้อนความเป็น gaming
- UX ที่เน้นความเร็วและประสิทธิภาพ

### 💫 **Modern Web Standards**
- Semantic HTML5
- CSS Grid และ Flexbox
- ES6+ TypeScript
- Progressive Enhancement
- Modern Build Tools (Vite)

### ⚡ **Performance First**
- Fast build times กับ Vite
- Optimized bundle splitting
- Lazy loading สำหรับ components
- Hardware-accelerated animations

## 📈 Future Enhancements

- 📊 **Analytics Dashboard** - สถิติการเข้าชม
- 🌍 **Multi-language Support** - รองรับหลายภาษา
- 🛒 **E-commerce Integration** - ระบบสั่งซื้อสินค้า
- 📧 **Newsletter System** - ระบบจดหมายข่าว
- 🔒 **User Authentication** - ระบบสมาชิก

## 👨‍💻 ผู้พัฒนา

โปรเจ็กต์นี้พัฒนาโดยนักพัฒนาเว็บไซต์ที่มีความสนใจในเทคโนโลยี Gaming และ Modern Web Development

- **Dev**: [Xeno](https://github.com/xenodeve)
- **Student ID**: s0330250
- **Repository**: [Hype-Macro_Vite](https://github.com/xenodeve/Hype-Macro_Vite)

## 📜 License

© 2025 HYPE-MACRO. สงวนลิขสิทธิ์ทุกประการ

---

### 🚀 Happy Coding! 🎮
